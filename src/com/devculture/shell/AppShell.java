package com.devculture.shell;

class AppShell {
	
	public final static void main(String[] args) {
		// TODO: based on the args, we can determine which application to run
		AppPortal.OpenApplication(AppPortal.AppName.TextureAtlasCreator);
	}

}

